<Page
    x:Name="pageRoot"
    x:Class="SYNAPSE.TimeLine"
    DataContext="{Binding DefaultViewModel, RelativeSource={RelativeSource Self}}"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SYNAPSE"
    xmlns:common="using:SYNAPSE.Common"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Page.Resources>
        <!-- TODO: Delete this line if the key AppName is declared in App.xaml -->
        <x:String x:Key="AppName">SYNAPSE</x:String>
    </Page.Resources>
    <Page.TopAppBar>
        <CommandBar>
            <AppBarButton Label="設定" Icon="Setting">
                <AppBarButton.Flyout>
                    <MenuFlyout>
                        <MenuFlyoutItem x:ConnectionId='1' Text="Profile"                             ></MenuFlyoutItem>
                        <MenuFlyoutItem x:ConnectionId='2' Text="Log Out"                               ></MenuFlyoutItem>
                    </MenuFlyout>
                </AppBarButton.Flyout>
            </AppBarButton>
        </CommandBar>
    </Page.TopAppBar>

    <!--
        This grid acts as a root panel for the page that defines two rows:
        * Row 0 contains the back button and page title
        * Row 1 contains the rest of the page layout
    -->
    <Grid Background="#FF0099FF">
        <Grid.ChildrenTransitions>
            <TransitionCollection>
                <EntranceThemeTransition/>
            </TransitionCollection>
        </Grid.ChildrenTransitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="140"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Back button and page title -->
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="120"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Button x:Name="backButton" Margin="39,59,39,0" Command="{Binding NavigationHelper.GoBackCommand, ElementName=pageRoot}"
                        Style="{StaticResource NavigationBackButtonNormalStyle}"
                        VerticalAlignment="Top"
                        AutomationProperties.Name="Back"
                        AutomationProperties.AutomationId="BackButton"
                        AutomationProperties.ItemType="Navigation Button" BorderBrush="White" Foreground="White" Background="Transparent"/>
            <TextBlock x:Name="pageTitle" Text="{StaticResource AppName}" Style="{StaticResource HeaderTextBlockStyle}" Grid.Column="1" 
                        IsHitTestVisible="false" TextWrapping="NoWrap" VerticalAlignment="Bottom" Margin="0,0,30,40" Foreground="White"/>
        </Grid>
        <TextBlock HorizontalAlignment="Left" Margin="93,36,0,0" Grid.Row="1" TextWrapping="Wrap" Text="つぶやく" VerticalAlignment="Top" Height="31" Width="130" FontSize="30" Foreground="White"/>
        <TextBox x:ConnectionId='3' x:Name="tweetBox" HorizontalAlignment="Left" Margin="93,67,0,0" Grid.Row="1" TextWrapping="Wrap" VerticalAlignment="Top" Height="172" Width="330"                    Background="#F2F2F2F2"/>
        <Button x:ConnectionId='4' Content="つぶやく" HorizontalAlignment="Left" Margin="346,241,0,0" Grid.Row="1" VerticalAlignment="Top"                          Background="Transparent" BorderBrush="White" Foreground="White" BorderThickness="2"/>
        <ListView x:Name="timeline" HorizontalAlignment="Left" Height="472" Margin="428,67,0,0" Grid.Row="1" VerticalAlignment="Top" Width="826" Foreground="White">
            <ListView.Background>
                <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                    <GradientStop Color="#FF0099FF" Offset="1"/>
                    <GradientStop Color="#F2F2F2F2" Offset="0.936"/>
                    <GradientStop Color="#F2F2F2F2" Offset="0.845"/>
                </LinearGradientBrush>
            </ListView.Background>
            <ListView.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Vertical">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{Binding Year}" Foreground="Chocolate" ></TextBlock>
                            <TextBlock Text="{Binding Month}" Foreground="Chocolate"></TextBlock>
                            <TextBlock Text="{Binding Day}" Foreground="Chocolate"></TextBlock>
                            <TextBlock Text="{Binding Hour}" Foreground="Chocolate"></TextBlock>
                            <TextBlock Text="{Binding Minute}" Foreground="Chocolate"></TextBlock>
                            <TextBlock Text="{Binding Second}" Foreground="Chocolate"></TextBlock>
                        </StackPanel>
                        <TextBlock Text="{Binding NickName}" Foreground="DarkBlue" FontSize="20"></TextBlock>
                        <TextBlock Text="{Binding Tweet}" Foreground="Black" FontSize="30" TextWrapping="Wrap"></TextBlock>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        <TextBlock HorizontalAlignment="Left" Margin="428,24,0,0" Grid.Row="1" TextWrapping="Wrap" Text="Timeline" VerticalAlignment="Top" FontSize="36" Foreground="White"/>
    </Grid>
</Page>

