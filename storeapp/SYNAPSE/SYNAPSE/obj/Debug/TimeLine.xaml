<Page
    x:Name="pageRoot"
    x:Class="SYNAPSE.TimeLine"
    DataContext="{Binding DefaultViewModel, RelativeSource={RelativeSource Self}}"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SYNAPSE"
    xmlns:common="using:SYNAPSE.Common"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Page.Resources>
        <!-- TODO: Delete this line if the key AppName is declared in App.xaml -->
        <x:String x:Key="AppName">My Application</x:String>
    </Page.Resources>
    <Page.TopAppBar>
        <CommandBar>
            <AppBarButton x:ConnectionId='1' Label="Main Page" Icon="Back"                            />
        </CommandBar>
    </Page.TopAppBar>

    <!--
        This grid acts as a root panel for the page that defines two rows:
        * Row 0 contains the back button and page title
        * Row 1 contains the rest of the page layout
    -->
    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.ChildrenTransitions>
            <TransitionCollection>
                <EntranceThemeTransition/>
            </TransitionCollection>
        </Grid.ChildrenTransitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="140"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Back button and page title -->
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="120"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Button x:Name="backButton" Margin="39,59,39,0" Command="{Binding NavigationHelper.GoBackCommand, ElementName=pageRoot}"
                        Style="{StaticResource NavigationBackButtonNormalStyle}"
                        VerticalAlignment="Top"
                        AutomationProperties.Name="Back"
                        AutomationProperties.AutomationId="BackButton"
                        AutomationProperties.ItemType="Navigation Button"/>
            <TextBlock x:Name="pageTitle" Text="{StaticResource AppName}" Style="{StaticResource HeaderTextBlockStyle}" Grid.Column="1" 
                        IsHitTestVisible="false" TextWrapping="NoWrap" VerticalAlignment="Bottom" Margin="0,0,30,40"/>
        </Grid>
        <TextBlock HorizontalAlignment="Left" Margin="93,36,0,0" Grid.Row="1" TextWrapping="Wrap" Text="つぶやく" VerticalAlignment="Top" Height="31" Width="130" FontSize="30"/>
        <TextBox x:ConnectionId='2' x:Name="tweetBox" HorizontalAlignment="Left" Margin="93,67,0,0" Grid.Row="1" TextWrapping="Wrap" VerticalAlignment="Top" Height="172" Width="330"                   />
        <Button x:ConnectionId='3' Content="つぶやく" HorizontalAlignment="Left" Margin="350,241,0,0" Grid.Row="1" VerticalAlignment="Top"                         />
        <Button x:ConnectionId='4' x:Name="get" Content="Get" HorizontalAlignment="Left" Margin="1208,551,0,0" Grid.Row="1" VerticalAlignment="Top"                       />
        <ListView x:Name="timeline" HorizontalAlignment="Left" Height="472" Margin="428,67,0,0" Grid.Row="1" VerticalAlignment="Top" Width="933">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Vertical">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{Binding Year}" Foreground="Chocolate"></TextBlock>
                            <TextBlock Text="{Binding Month}" Foreground="Chocolate"></TextBlock>
                            <TextBlock Text="{Binding Day}" Foreground="Chocolate"></TextBlock>
                            <TextBlock Text="{Binding Hour}" Foreground="Chocolate"></TextBlock>
                            <TextBlock Text="{Binding Minute}" Foreground="Chocolate"></TextBlock>
                            <TextBlock Text="{Binding Second}" Foreground="Chocolate"></TextBlock>
                        </StackPanel>
                        <TextBlock Text="{Binding NickName}" Foreground="Aqua"></TextBlock>
                        <TextBlock Text="{Binding Tweet}"></TextBlock>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        <Image x:Name="testimage" HorizontalAlignment="Left" Height="64" Margin="179,340,0,0" Grid.Row="1" VerticalAlignment="Top" Width="65"/>
        <Button x:ConnectionId='5' Content="test" HorizontalAlignment="Left" Margin="171,417,0,0" Grid.Row="1" VerticalAlignment="Top"                        />
        <CaptureElement HorizontalAlignment="Left" Height="100" Margin="584,36,0,0" Grid.Row="1" VerticalAlignment="Top" Width="100"/>
    </Grid>
</Page>

